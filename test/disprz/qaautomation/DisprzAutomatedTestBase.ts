/**
 * This is the base class for the automation framework to initialize the test steps
 * @author - Arun Sundar
 */

abstract class DisprzAutomatedTestBase implements UIPage{
//     /**
//     * This method will perform validations for NonActionable components
//     *
//     */
//     protected verifyNonActionableElements() {

//     }

    /**
     * This method will perform validations for Actionable components
     */
    // public performAction(page,elementName,testData) {
    //     actionableElement = page.elementName;
    //     var actionableElement;
    //     if (null == actionableElement) {
    //         // logger.error("Actionable element " + elementName + " not found in page " + page.name());
    //         // report.reportFail("Actionable element " + elementName + " not found in page " + page.name());
    //         return null;
    //     } else {
    //         return this.performActions();
    //     }
    // }
}